# AI 协作契约（AltShort）

## 任务执行顺序（必须遵守）

1. **冻结接口**：先稳定包边界与函数签名，再写实现。  
2. **搭骨架**：目录、入口、`NewXxx` / `Run` 占位、配置与 README。  
3. **补依赖**：按 Phase 引入最小的第三方库与基础设施。  
4. **补实现**：按模块增量填充，避免跨层一口吃成胖子。  
5. **补测试**：先关键纯函数与配置加载，再集成与端到端。

## 变更规则

- **一次只做一个 Phase**：同一提交/会话内不跨 Phase 混改（例如同时大改策略签名又改 Agent 协议）。
- 策略相关变更默认 **接口向后兼容**；破坏性变更必须显式记录在提交说明并单独评审。

## 每次完成后的产物说明（请附在回复末）

- **新增文件**：列路径。  
- **变更文件**：列路径与一句话原因。  
- **未完成项**：列清晰的 TODO（若暂无则写“无”）。  
- **下一步建议**：单一、可执行的下一阶段入口。

## 代码风格（Go）

- **命名**：包名短小、全小写；导出符号表意完整，避免缩写泛滥。  
- **结构体字段**：语义清晰，布尔用 `Is`/`Has`/`Enable` 等前缀。  
- **函数职责**：单一职责；过长则拆。  
- **注释**：只解释「为什么」和约束背景，不重复代码字面含义。
